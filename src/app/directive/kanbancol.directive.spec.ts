import { KanbancolDirective } from './kanbancol.directive';

describe('KanbancolDirective', () => {
  it('should create an instance', () => {
    const directive = new KanbancolDirective();
    expect(directive).toBeTruthy();
  });
});
